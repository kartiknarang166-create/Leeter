// This postcss.config.js is for the ROOT Next.js project (legacy).
// The FRONTEND folder (Vite + Tailwind v4) uses @tailwindcss/vite plugin
// and does NOT need PostCSS config — it's handled automatically.
module.exports = {
  plugins: {},
};
